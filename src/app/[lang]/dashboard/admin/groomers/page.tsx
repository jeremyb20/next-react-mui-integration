import GroomersView from './_components/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Admin Users',
};

export default function GroomersPage() {
  return <GroomersView />;
}
